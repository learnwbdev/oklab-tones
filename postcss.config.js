// import cssnano from "cssnano";
import postcssNesting from "postcss-nesting";

export default {
  plugins: [
    // cssnano({
    //     preset: "default",
    // }),
    postcssNesting({
      /* plugin options */
    }),
  ],
};
